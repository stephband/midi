import { test } from '../../fn/module.js';
import {

} from '../modules/midi.js';

test('MIDI midi', function(run, log, fixture) {
	//run('createMessage()', function(equals, done) {
    //    equals(expected, returned);
    //    done();
	//});
});
